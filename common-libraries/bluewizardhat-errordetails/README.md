Common Libraries - Error Details
==================================================================================================

Common error message hierarchy. If all services uses the same error format that can be propagated when services calls each other.
